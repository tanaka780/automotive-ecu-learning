# 学習計画

---

## Phase1

### 理解目標

- struct でデータをまとめる意味を理解する
- .c / .h でモジュールを分ける意味を理解する
- main.c を見てデータの流れを追える

### タスク

- [x] センサシミュレーション基本実装
- [x] 統計モジュール追加
- [x] アラートモジュール追加
- [x] センサ状態モジュール追加（status.c / enum の導入）
- [x] 固定幅整数型の導入（uint8_t / uint16_t / uint32_t への置き換え）

---

## Phase2

### 選定理由

- 責務分離が崩れない：diag.cは「記録と表示」のみを担当し、既存の閾値判定（alert.c/status.c）と責務が重複しない。status.cの分類結果を読むだけの設計にできる
- 固定サイズ配列で動的確保（malloc）をしない設計が、メモリ使用量を予測可能にする必要がある組み込み・車載分野の実態に合う
- センサ別コード、フリーズフレーム、アクティブ/ヒストリー区分など、実際のDTC概念との対応関係が明確

### 理解目標

- DTC（故障診断コード）という車載診断の基本的な考え方を理解する
- 既存モジュール（status.c）の分類結果を読むだけで新しいモジュールを追加する設計を理解する
- 配列を使ったデータの記録・管理の基礎を理解する

### タスク

- [x] DTCモジュール追加（diag.h / diag.c）、CRITICAL検出時に原因センサを区別して記録
- [x] 全サンプル実行後にDTC一覧を表示

### Phase2拡張バックログ（優先順位）

上記タスク完了時点では「配列を使ったデータの記録・管理の基礎を理解する」という理解目標が未達成、かつ選定理由にあったフリーズフレーム・アクティブ/ヒストリーの概念も未着手だったため、Phase2をもう少し深める。

以下は優先順位付きのバックログであり、固定の日程ではない。各回の作業範囲は、その日のコード・ドキュメント・理解状況を確認してから判断する（CLAUDE.mdの方針に従う）。

- [x] DTCの保存方法を、個別変数から配列に直す（enum + struct + 配列）
- [x] DTCの重複登録防止の動作確認（diag_check()は前回/今回比較によりPhase2タスク完了時点で実装済み。新規実装ではなく、説明できるかの確認が中心）
- [x] active / history の簡易管理を追加する
- [x] フリーズフレームを1件だけ保存する機能を追加する
- [x] DTC / alert / status の責務を整理し、ドキュメントに反映する（project_context.md参照）

上記に加えて、バックログの優先順位とは別に、固定値データによるdiag.cの動作確認テスト（`test/test_diag.c`、`make test`）を追加した。

当初は本項目に「logger」も含めていたが、DTC管理（Phase2のテーマ）とは別軸の変更（複数モジュールにまたがる出力層の共通化）と判断し、次のPhaseへ切り出した。

### 設計改善（後日）

- [x] `SensorStatus`を`speed_level`/`rpm_level`/`temp_level`の個別フィールドから`SensorLevel levels[SENSOR_COUNT]`の配列に変更し、`SensorId`をdiag.hからsensor.hに移動（diag.cの一時配列への詰め替え回避コードを解消）

---

## Phase3：logger（出力層の共通化）

### 選定理由

- 現状、sensor.c / stats.c / alert.c / status.c / diag.c がそれぞれ自前で `printf` による表示を持っており、値の更新・判定という本来の責務に加えて、出力（表示）の責務も個別に抱えている
- Phase2（DTC管理）とはテーマが異なる横断的な変更であり、Phase2拡張バックログには含めず独立したPhaseとして扱うこととした

### 理解目標

- 可変長引数（`<stdarg.h>`）を使う設計と使わない設計を比較し、車載関連プログラムとしてより適切な方を選べる
- 出力（表示）の責務を独立したモジュールに切り出す設計判断ができる
- 複数モジュールへの横断的な変更を、1モジュールずつ検証しながら安全に進められる

### タスク

- [x] 可変長引数のメリット・デメリットを比較し、使わない設計（`snprintf`で文字列を組み立ててから渡す）を採用
- [x] logger.h / logger.cを新規作成（`log_print` / `log_print_tagged`）
- [x] sensor.c → status.c → stats.c → alert.c → diag.c の順で1モジュールずつ移行（都度ビルド・テスト・実行確認）

### 移行漏れの修正（後日）

- [x] main.cに残っていた`printf`直呼び出し（サンプル見出し）をlogger経由に統一

---

## Phase4：状態遷移（イグニッション状態管理）

### 選定理由

- 新しいテーマに進む前に、これまでの学習で扱っていない基礎的な内容（エラーハンドリング／ビット演算／文字列操作）が無いかをAIと確認したところ、いずれも今のモジュールだけでは導入する動機が不自然で、他の未実装機能とセットで実施すべきと判断した（下記「保留中の候補」参照）
- 保留中の候補（DTCの永続化／状態遷移／診断コマンド入力）のうち、状態遷移（イグニッション状態管理）だけが他の機能待ちになっておらず、既存のenum・状態判定パターン（status.c / diag.c）の延長で着手できたため、次のテーマに選定した

### 理解目標

- 実車の電源状態（イグニッション）という新しい軸のデータを、既存の責務分割にどう位置づけるかを判断できる
- 前回状態との比較によるエッジ検出パターン（diag.cで学んだもの）を、別のテーマに応用できる

### タスク

- [x] 独立モジュール（ignition.h / ignition.c）を新設し、`IgnitionState`（OFF/ON）を`Ignition`構造体（current/previous）で保持
- [x] sensor.cのランダム更新パターンで状態を更新（`ignition_update`）
- [x] diag.cのエッジ検出パターンを流用し、遷移した瞬間だけイベント表示（`ignition_check`）
- [x] 現在の状態を毎回表示（`ignition_print`）
- [x] OFF中の連携方針を決定：sensor_update/status_check/diag_check/alert_check/stats_updateを全てスキップする（ECUが通電していない状態を再現）
- [x] サイクルをまたぐDTCのactive/history・統計のリセット可否を決定：どちらもリセットしない（通算で継続する）。DTCは実車でも電源断をまたいで保持されるのが本質的な仕様のため現状維持が適切、statsも今回は現状維持を選択

---

## 保留中の候補（優先順位未定）

- **DTCの永続化**（NVM/EEPROMシミュレーション、ファイルI/O）: エラーハンドリング（戻り値チェック）とセットで実施する。現状の関数はどれも実際には失敗しない（malloc・ファイルI/O等が無い）ため、単独で戻り値チェックを導入すると不自然な実装になる
- **診断コマンド入力**（UDS風のDTCクリア）: 文字列操作（`<string.h>`）とセットで実施する。現状のモジュールに文字列比較・分割・コピーが必要な処理がない
- **ビット演算**（車載のレジスタ・プロトコル操作で頻出）: `SensorStatus`等の内部データをビット詰めする動機（メモリ削減）は、実際には外部境界（通信・永続化）が無いと固められない。実際のDTCステータスバイト（ISO 14229等）は本プロジェクトの案とは異なるビット配置であり、先に詰めても後で規格に合わせて作り直す可能性が高いため、外部境界ができるまで保留する
