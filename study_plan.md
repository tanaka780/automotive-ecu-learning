# 学習計画

---

## Phase1

### 理解目標

- struct でデータをまとめる意味を理解する
- .c / .h でモジュールを分ける意味を理解する
- main.c を見てデータの流れを追える

### タスク

- [x] センサシミュレーション基本実装
- [x] 統計モジュール追加
- [x] アラートモジュール追加
- [x] センサ状態モジュール追加（status.c / enum の導入）
- [x] 固定幅整数型の導入（uint8_t / uint16_t / uint32_t への置き換え）

---

## Phase2

### 選定理由

- 責務分離が崩れない：diag.cは「記録と表示」のみを担当し、既存の閾値判定（alert.c/status.c）と責務が重複しない。status.cの分類結果を読むだけの設計にできる
- 固定サイズ配列で動的確保（malloc）をしない設計が、メモリ使用量を予測可能にする必要がある組み込み・車載分野の実態に合う
- センサ別コード、フリーズフレーム、アクティブ/ヒストリー区分など、実際のDTC概念との対応関係が明確

### 理解目標

- DTC（故障診断コード）という車載診断の基本的な考え方を理解する
- 既存モジュール（status.c）の分類結果を読むだけで新しいモジュールを追加する設計を理解する
- 配列を使ったデータの記録・管理の基礎を理解する

### タスク

- [x] DTCモジュール追加（diag.h / diag.c）、CRITICAL検出時に原因センサを区別して記録
- [x] 全サンプル実行後にDTC一覧を表示

### Phase2拡張バックログ（優先順位）

上記タスク完了時点では「配列を使ったデータの記録・管理の基礎を理解する」という理解目標が未達成、かつ選定理由にあったフリーズフレーム・アクティブ/ヒストリーの概念も未着手だったため、Phase2をもう少し深める。

以下は優先順位付きのバックログであり、固定の日程ではない。各回の作業範囲は、その日のコード・ドキュメント・理解状況を確認してから判断する（CLAUDE.mdの方針に従う）。

- [x] DTCの保存方法を、個別変数から配列に直す（enum + struct + 配列）
- [x] DTCの重複登録防止の動作確認（diag_check()は前回/今回比較によりPhase2タスク完了時点で実装済み。新規実装ではなく、説明できるかの確認が中心）
- [x] active / history の簡易管理を追加する
- [x] フリーズフレームを1件だけ保存する機能を追加する
- [x] DTC / alert / status の責務を整理し、ドキュメントに反映する（project_context.md参照）

上記に加えて、バックログの優先順位とは別に、固定値データによるdiag.cの動作確認テスト（`test/test_diag.c`、`make test`）を追加した。

当初は本項目に「logger」も含めていたが、DTC管理（Phase2のテーマ）とは別軸の変更（複数モジュールにまたがる出力層の共通化）と判断し、次のPhaseへ切り出した。

### 設計改善（後日）

- [x] `SensorStatus`を`speed_level`/`rpm_level`/`temp_level`の個別フィールドから`SensorLevel levels[SENSOR_COUNT]`の配列に変更し、`SensorId`をdiag.hからsensor.hに移動（diag.cの一時配列への詰め替え回避コードを解消）

---

## Phase3：logger（出力層の共通化）

### 選定理由

- 現状、sensor.c / stats.c / alert.c / status.c / diag.c がそれぞれ自前で `printf` による表示を持っており、値の更新・判定という本来の責務に加えて、出力（表示）の責務も個別に抱えている
- Phase2（DTC管理）とはテーマが異なる横断的な変更であり、Phase2拡張バックログには含めず独立したPhaseとして扱うこととした

### 理解目標

- 可変長引数（`<stdarg.h>`）を使う設計と使わない設計を比較し、車載関連プログラムとしてより適切な方を選べる
- 出力（表示）の責務を独立したモジュールに切り出す設計判断ができる
- 複数モジュールへの横断的な変更を、1モジュールずつ検証しながら安全に進められる

### タスク

- [x] 可変長引数のメリット・デメリットを比較し、使わない設計（`snprintf`で文字列を組み立ててから渡す）を採用
- [x] logger.h / logger.cを新規作成（`log_print` / `log_print_tagged`）
- [x] sensor.c → status.c → stats.c → alert.c → diag.c の順で1モジュールずつ移行（都度ビルド・テスト・実行確認）

### 移行漏れの修正（後日）

- [x] main.cに残っていた`printf`直呼び出し（サンプル見出し）をlogger経由に統一

---

## Phase4：状態遷移（イグニッション状態管理）

### 選定理由

- 新しいテーマに進む前に、これまでの学習で扱っていない基礎的な内容（エラーハンドリング／ビット演算／文字列操作）が無いかをAIと確認したところ、いずれも今のモジュールだけでは導入する動機が不自然で、他の未実装機能とセットで実施すべきと判断した（下記「保留中の候補」参照）
- 保留中の候補（DTCの永続化／状態遷移／診断コマンド入力）のうち、状態遷移（イグニッション状態管理）だけが他の機能待ちになっておらず、既存のenum・状態判定パターン（status.c / diag.c）の延長で着手できたため、次のテーマに選定した

### 理解目標

- 実車の電源状態（イグニッション）という新しい軸のデータを、既存の責務分割にどう位置づけるかを判断できる
- 前回状態との比較によるエッジ検出パターン（diag.cで学んだもの）を、別のテーマに応用できる

### タスク

- [x] 独立モジュール（ignition.h / ignition.c）を新設し、`IgnitionState`（OFF/ON）を`Ignition`構造体（current/previous）で保持
- [x] sensor.cのランダム更新パターンで状態を更新（`ignition_update`）
- [x] diag.cのエッジ検出パターンを流用し、遷移した瞬間だけイベント表示（`ignition_check`）
- [x] 現在の状態を毎回表示（`ignition_print`）
- [x] OFF中の連携方針を決定：sensor_update/status_check/diag_check/alert_check/stats_updateを全てスキップする（ECUが通電していない状態を再現）
- [x] サイクルをまたぐDTCのactive/history・統計のリセット可否を決定：どちらもリセットしない（通算で継続する）。DTCは実車でも電源断をまたいで保持されるのが本質的な仕様のため現状維持が適切、statsも今回は現状維持を選択

---

## Phase5：DTCの永続化（ファイルI/O）

### 選定理由

- Day12時点でエラーハンドリング（戻り値チェック）を保留にしていた理由は「既存の関数がどれも実際には失敗しない（malloc・ファイルI/O等が無いため）」ことだった。DTCの永続化にファイルI/Oを導入すれば、「ファイルが無い」「内容が壊れている」という実際に失敗しうる処理が初めて生まれ、エラーハンドリングを自然な形で導入できる
- 既存モジュールが実践してきた「更新・判定」と「表示」の分離（sensor.c、diag.c等）、Phase3で導入した「組み立てとI/Oの分離」（logger.c）という2つのパターンを、ファイルI/Oという新しい境界にもそのまま応用できる

### 理解目標

- ファイルI/O（`fopen`/`fwrite`/`fread`/`fclose`）を、正常系だけでなく異常系（ファイルが無い、内容が壊れている）も含めて扱えるようになる
- ロジック（`DtcRecord`とテキストの変換）とI/O（実際のファイル読み書き）を分離する設計判断が、エラーハンドリングの範囲整理にどう効くかを説明できる

### タスク

- [x] DTCの永続化を実装する：`persist.h` / `persist.c`を新規作成し、`DtcRecord`の保存・読み込みを`main.c`（起動時読み込み・終了時保存）に組み込んだ。保存形式はテキスト、`previous`（エッジ検出用の前回状態）は保存対象に含めない設計にした
- [x] エラーハンドリングを導入する：ファイルが無い場合・内容が壊れている場合・書き込みに失敗した場合を戻り値で判定し、`[PERSIST]`タグ付きログで正常系・異常系を区別して表示できるようにした。「内容が壊れている場合」は`test/test_persist.c`で、値の個数が足りないケース・数値でない文字列のケースを実際に確認した。「値の個数は揃っているが意味的に不正な値」は今回のテスト範囲外とし、既知の制約として残す（README.md参照）
- [x] `test/test_persist.c`を新規作成し、保存→読み込みの往復一致（正常系、previousがNORMALにリセットされることも含む）、ファイルが無い場合、内容が壊れている場合（異常系：値不足・数値以外）を自動テストする。本番の`dtc_data.txt`を壊さないよう、テスト専用ファイル名（`test_dtc_data.txt`）を使い、テスト終了時に削除する設計にした
- [x] `test_diag.c`と`test_persist.c`で重複する`check()`等のテスト共通処理を`test_common.h` / `.c`に切り出す：`test_check` / `test_run_sample` / `test_feed` / `test_summary`として共通化し、両ファイルから呼び出すようにした（`g_pass`/`g_fail`は`test_common.c`内の`static`変数のまま。`test_diag`/`test_persist`は別々の実行ファイルなので問題にならない）

### Phase5拡張バックログ（優先順位）

Day15時点でPhase5本来のタスク（永続化実装・エラーハンドリング・test_persist.c作成・test_common整理）は完了していたが、Day15の日誌に「次回やること」としてモジュール別の自動テスト拡大（まずはstats.cから）を記録していた。DTC永続化というPhase5本来のテーマそのものではないが、Day15のセッションの続きとして挙がった作業のため、Phase5の拡張バックログに位置づける。

- [x] stats.cへの自動テスト追加（`test/test_stats.c`新規作成）。`test_common.h` / `.c`は変更せず、サンプル投入用のヘルパーは`test_stats.c`内にローカルで定義した（`test_common.c`の`test_feed`は`DtcRecord`前提の作りであり、変更するとdiag/persistの既存テストに影響しうるため）
- [x] alert.c / ignition.c / sensor.c / logger.cへの自動テスト追加の要否を評価した：alert.c / ignition.c（`ignition_check`）は判定結果を構造体に書き込まず標準出力するだけのため、これまでの「構造体の中身をcheck()で確認する」手法が使えず、標準出力キャプチャという新しい技術が必要と判断した。Phase2→Phase3（logger.c切り出し）の前例に倣い、Phase5拡張には含めず独立したPhase6として扱うことにした（詳細はPhase6参照）。sensor.cはロジックがほぼなく優先度が低いため引き続き対象外、logger.cはPhase6のテストの土台になるため任意で検討する

---

## Phase6：標準出力を伴う判定処理のテスト（標準出力キャプチャ）

### 選定理由

- alert.c（`alert_check`）とignition.c（`ignition_check`）は、判定結果を構造体に書き込まず標準出力に表示するだけの設計になっており、diag.c/persist.c/stats.cのテストで使ってきた「構造体の中身をcheck()で確認する」手法がそのまま使えない
- Phase2からPhase3（logger.c切り出し）が独立したときと同じ理由（新しい技術テーマが入る横断的な変更）で、Phase5拡張バックログには含めず独立したPhaseとして扱うこととした

### 理解目標

- 標準出力をキャプチャして検証する手法（`freopen`等）を使えるようになる
- 「判定結果を状態（構造体）で確認する」パターンと「出力そのものを確認する」パターンの違いを説明できる

### タスク

- [x] 標準出力キャプチャの手法（`freopen`等）を確認する：`freopen`だけでは標準出力を元の端末に戻す手段が残らないため、`dup`で元のfdを退避し`dup2`で復元する方式を採用した
- [x] `test/test_alert.c`を新規作成し、`alert_check`の閾値境界（超えない/超える）で警告が正しく出力されるか確認する。Makefileにも`test_alert`ターゲットを追加し`test`/`clean`に組み込み済み
- [x] `test/test_ignition.c`を新規作成し、`ignition_check`が遷移した瞬間だけイベントを表示し、遷移していないときは表示しないことを確認する
- [x] Makefileを更新し、`test_ignition`を`test`ターゲットに組み込む
- [ ] （検討）logger.c（`log_print` / `log_print_tagged`）の出力フォーマットも合わせて確認するかを判断する

---

## Phase7：診断コマンド入力（UDS風のDTCクリア）

### 選定理由

- 「保留中の候補」に元々挙げていた項目。文字列操作（`<string.h>`）とセットで実施する動機として、既存モジュールに文字列比較・分割・コピーが必要な処理がなかったため保留にしていたが、DTC管理（diag.c）とDTC永続化（persist.c）が実装済みの今なら、UDS（統一診断サービス）のDTCクリア相当のコマンドを再現する土台が揃っている
- 文字列操作というPhase4時点で識別していた未実施の基礎テーマ（エラーハンドリング／ビット演算／文字列操作のうち、エラーハンドリングとビット演算相当のテーマは既に扱った）にも合致する

### 理解目標

- 文字列比較（`<string.h>`の`strcmp`等）を使ってコマンド入力を処理する基本的な設計を理解する
- 実際の車載診断（UDS）における「DTCクリア」という操作の考え方を、簡易的な形で再現できる
- 新しい入力の受け付け方（コマンド入力）を、既存の責務分割にどう位置づけるかを判断できる

### タスク

- [x] 文字列比較（`<string.h>`）を使ったコマンド入力の受け付け方を検討する：受け付けるタイミング（サンプルループ終了後、`diag_print`の直後・`persist_save_dtc`の直前に1回のみ）、実装モジュール（新規`cmd.h`/`cmd.c`に「入力読み取り」と「文字列解釈」を分離）、判定基準（`"clear"`の完全一致、大文字小文字を区別、前後の空白はトリムしない、空文字列・EOFは何もしない）を整理した
- [x] "clear"相当のコマンドでDTC記録をリセットする機能を実装する（対象モジュールは既存の責務分割を確認した上で判断する）：実際に`DtcRecord`を書き換える`diag_clear`は既存の責務分割どおり`diag.c`に置き、内部で`diag_init`を呼び出す形にして初期化処理の重複を避けた。`cmd.c`は`diag_clear`を呼び出すだけで、`DtcRecord`を直接は書き換えない
- [x] persist.c（DTCの永続化）との整合をどう取るかを検討する（クリア後にファイルへどう反映するか）：クリア操作と最終保存（`persist_save_dtc`）の間に他の処理を挟まない配置にしたため、`persist.c`は変更せず既存の終了時保存にそのまま任せることにした
- [x] 自動テストの要否・方法を検討する：`diag_clear`と`cmd_dispatch`（文字列解釈部分）は`test/test_cmd.c`で自動テスト化した。標準入力を読むだけの`cmd_read_line`は対象外とし、`make run`での実行確認で扱うことにした

### Phase7拡張バックログ（優先順位）

Day19時点でPhase7本来のタスク（コマンド受け付け方の検討・`clear`機能の実装・persist.cとの整合・自動テスト）は完了した。検討の過程で、コマンドを受け付けるタイミングとして、今回採用した「サンプルループ終了後に1回だけ」ではなく、「イグニッションOFFへ遷移した瞬間に受け付ける」案（実際に駐車中の車両にスキャンツールを接続する状況に近い）も比較したが、今回は技術範囲を抑えるため見送った。

さらに、実装後に「UDS風と呼ぶには実際のUDS（ISO 14229）の仕組みに対してどれくらい浅いか」を振り返り、今回は`"clear"`という単一コマンドで全DTCを一括クリアするだけで、実際のUDSが持つ「クリア範囲の指定（groupOfDTC）」「拒否理由の区別（ネガティブレスポンスコード）」「特定条件でしか受け付けない仕組み（セッション/認可）」に相当する概念が無いことを確認した。これらはいずれも新しい技術テーマ（Phase6のような標準出力キャプチャ等）を必要とせず、既存の技術（文字列比較・enum・構造体の状態参照）の延長で対応できるため、独立Phaseに切り出さずPhase7拡張バックログに位置づける。以下は優先順位未定の拡張候補。

- [ ] イグニッションOFFへの遷移時にコマンドを受け付けるよう変更する（`ignition_check`と同じエッジ検出パターンを応用し、OFFが続く間ずっとではなく遷移した瞬間だけ受け付ける）：Day21時点で未着手。検討の結果、`cmd_read_line`が標準入力からのブロッキング読み込みであるため、ループ内で呼ぶとOFF遷移のたびにプログラム全体が入力待ちで止まり、1秒間隔のサンプルループという既存の時間モデルが崩れるという課題を認識した。Phase8着手前の検討で、この課題への対処（非ブロッキング化等）は保留中の候補にあるTimer/Schedulerで扱う時間モデルの話と重なると判断し、それらのPhase以降に先送りすることにした（今回は見送り、実装しない）
- [ ] 上記に伴いクリア操作と最終保存の間隔が生まれるため、`persist_save_dtc`をクリア直後に呼ぶかどうか（現在は終了時保存に任せる案を採用）を再検討する：上記の受付タイミング変更が前提の論点のため、それが先送りされている間はこちらも保留
- [x] クリア対象をセンサ単位で指定できるようにする（例：`"clear speed"`のように対象を指定。実際のUDSのgroupOfDTC相当の簡易再現）：`diag.c`に`diag_clear_sensor`を追加し、指定センサ1件分のエントリだけをリセットするようにした。フリーズフレームは1件しか保持しないため、その原因が指定センサと一致する場合だけ合わせてリセットし、別センサが原因なら保持する設計にした。`cmd.c`側は`sscanf`で最大3トークンに分割し、`"clear"`（1トークン）と`"clear <センサ名>"`（2トークン）を区別する形にした
- [x] 想定外の入力を一律`[CMD] Unknown command`とせず、拒否理由を区別して表示する（実際のUDSのネガティブレスポンスコードの簡易再現）：センサ単位クリアの実装に伴って必要になった分だけ対応した。`"clear"`に続くセンサ名が不正な場合は`[CMD] Unknown target for clear (use speed/rpm/temp)`、それ以外（`clear`以外のコマンド、空白のみの入力、3トークン以上の入力）は従来通り`[CMD] Unknown command`のままで、区別は2種類にとどまる
- [x] 特定条件（例：イグニッションOFF時のみ）でしかクリアを受け付けないようにする（実際のUDSのセッション/認可の考え方の簡易再現）：Day21で対応。上記「OFFへの遷移時に受け付ける」（タイミングの変更）とは別の論点と整理し、受付タイミングは現状維持（サンプルループ終了後に1回）のまま、その時点の`ignition.current`がOFFかどうかだけを条件に追加した。`main.c`側で条件分岐し（既存のOFF中処理スキップと同じ判断軸）、ONの場合は`cmd.c`に新設した`cmd_notify_rejected`で`[CMD] Not accepted (ignition ON)`を表示するようにした

---

## Phase8：Config（閾値の外部化）

### 選定理由

- `alert.h`/`status.h`の閾値がマクロでコードに固定されており、値を変えるたびに再ビルドが必要になっている
- Phase5のpersist.cで学んだファイルI/Oをそのまま応用でき、新しい外部境界を増やさずに着手できる

### 理解目標

- 「コードに埋め込まれた値」と「外部ファイルから読み込む設定値」の違いを、実装の変更点として説明できる
- persist.cの「読み込み失敗時の扱い」を、DTC（記録データ）ではなく閾値（動作に必須なパラメータ）という性質の異なるデータに適用したときの違いを説明できる

### タスク

- [x] `config.h`/`config.c`を新規作成する：外部化対象は`alert.h`の3閾値+`status.h`の6閾値（計9個、`sensor.c`のセンサ値レンジは物理定数としての性質が強いため対象外）、ファイル形式は`KEY=VALUE`（persist.cでテキスト形式を選んだ「catで確認できる」理由に合わせ、位置固定より自己記述的な形式にする）、読み込み失敗時は`alert.h`/`status.h`の現行マクロ値にフォールバックする（persist.cの「読み込めなければ現状維持で継続する」設計に合わせる）。Makefile本体ターゲットに`config.c`を追加し、`main.c`から`config_init`/`config_load`/`config_print`を呼び出して読み込んだ値をログ出力する状態まで実装する（`alert_check`/`status_check`への引き渡しは次のタスクで行う）。ビルド警告なし、既存`make test`6ファイル計116件に影響なし、`config.txt`が無い状態で`alert.h`/`status.h`の現行値通りに表示されることを確認する
- [x] `alert.c`/`status.c`の関数シグネチャに`const ConfigData *`を追加してマクロ参照箇所を置き換え、`main.c`の`alert_check`/`status_check`呼び出しに`ConfigData`を渡すよう更新する。対象は`main.c`・`test_common.c`・`test_alert.c`に加え、`status_check`を直接呼んでいる`test_diag.c`（6箇所）も含める。個別対応はせず、`test_common.c`に共有のデフォルト`ConfigData`を返す`test_default_config()`を新設し、`test_diag.c`・`test_alert.c`から使い回す形にする。Makefileの6テストターゲット全てに`config.c`を追加する
- [x] 非デフォルトの`ConfigData`を渡したときに`alert_check`/`status_check`の判定が実際に変わることを確認する自動テストを追加する。既存の`test_alert.c`・`test_diag.c`にケースを追加する方針とし、新規`test_config.c`は作らない（`test_config.c`はファイルパース自体の検証用として下記タスクに切り分ける）
- [x] `test_config.c`を新規作成し、代表的なケース（正常系1つ、異常系2つ：不正行の無視・キー重複時の後勝ち）に絞って自動テストを追加する

---

## Phase9：Logger拡張（ログレベル）

### 選定理由

- 「保留中の候補」に元々挙げていた項目。単独では「ログを重要度で分類する動機」が薄かったが、Phase8でConfigの仕組み（`ConfigData`・`config_init`・`config_load`）が整ったことで、「表示するログの範囲を再ビルド無しで実行時に絞り込める」という具体的な使い道が生まれた
- 既存のログ出力（`log_print`/`log_print_tagged`）は`STATUS`/`ALERT`/`DTC`/`IGN`/`PERSIST`/`CMD`/`CONFIG`の7タグに分かれてはいるが、重要度（INFO/WARNING/ERROR相当）による分類は無く、常に全て表示される。これを見直す

### 理解目標

- 「出力先を表すタグ」と「メッセージの重要度を表すレベル」が別の分類軸であることを説明できる
- 各ログ呼び出しに埋め込む固定のレベル（コード側で決まる）と、`config.txt`で指定する実行時の表示閾値（利用者側で変えられる）という、2つの異なる「レベル」の役割の違いを説明できる
- Config機構（`config_init`→`config_load`での上書き、ファイル無し時のデフォルト継続）を、閾値以外の設定値（ログレベル）にも同じパターンで応用できることを確認する

### タスク

- [x] `logger.h`に`LogLevel`（`LOG_INFO`/`LOG_WARNING`/`LOG_ERROR`）を定義し`log_print_leveled`を新設する。`logger.c`に表示閾値の保持（`logger_set_level`）も実装し、`alert.c`で試験的に動作確認する
- [x] `ConfigData`に`log_level`フィールドを追加し、`config_init`でデフォルト`LOG_INFO`を設定、`config_load`の`apply_line`に`LOG_LEVEL`キー（数値0/1/2）を追加する
- [x] `main.c`の`config_load`直後に`logger_set_level(config.log_level)`を呼び出す配線を追加する
- [x] 残るログ呼び出し（`IGN`/`STATUS`/`DTC`/`PERSIST`/`CMD`/`CONFIG`）をPhase3の前例に倣って`log_print_leveled`に置き換える。`PERSIST`は保存失敗・データ破損時のみ`LOG_ERROR`とする（初回起動でファイル無しは正常な状態のため`LOG_INFO`。当初は達成可否だけで判断し両方ERRORにしていたが、実態に合わせて修正した）。`CONFIG`は`config_print`のみレベル化し、`config_load`自身の読み込み結果ログ2箇所は、その時点でまだ`log_level`が確定していないため`log_print_tagged`のまま残す

---

## 開発方針（Phase9以降）

Phase1〜9では、自動車関連プログラムに使われる個々の機能を、主に「C初心者として次に何を学ぶべきか」という学習順序の観点で選んで実装してきた。Phase9以降は、この学習順序だけでなく、完成目標であるECUソフトウェアシミュレータ（車両状態を模擬し、異常を検出・診断・記録し、電源再投入後も診断情報を保持できるECU）に本当に必要かどうかを主な選定基準とする。新しい技術要素の学習自体は今後も続く。

新しい機能を追加する際は、CLAUDE.md記載の判断順序（①ストーリー上の必要性→②既存責務への影響→③テスト方法→④Phase化）に従って検討する。

## 完成時に目指す車両シナリオ（概要）

- 正常走行：Ignition ON → センサ更新 → 状態判定 → 正常走行 → Ignition OFF
- 故障発生：走行中にセンサ異常 → Status/Alert → DTC登録 → Freeze Frame保存 → Logger
- 電源再投入：Ignition OFF時にDTC等の診断情報をNVMへ保存 → ECU再起動時にNVMから復元
- （将来）通信故障：ECU間CAN通信のTimeout/Invalid Data → Fault Detection → DTC

詳細な入出力・期待結果は、着手時に docs/scenarios.md として整理する（未着手）。

## 保留中の候補

Phase9着手後に何を実装するかの候補一覧。順序は現時点の見立てであり固定ではない。実装を進める中で「別のテーマが先に必要」と分かった場合は入れ替えてよい。

### Phase10以降（ラフな方向性、優先順位は固定ではない）

Phase1〜9のような選定理由・理解目標の詳細設計は、各Phaseの着手直前に行う。ここでは方向性のラベルのみを示す。着手前には必ず、上記の判断順序（①〜④）で本当に必要かを再検討する。

| Phase | テーマ | 目的（1〜2行） |
| --- | --- | --- |
| Phase10 | Python自動検証基盤 | Cの単体テストは残したまま、Pythonから`make test`を実行し結果を集計できるようにする |
| Phase11 | 車両シナリオの定義 | 正常走行／各センサ異常／DTC永続化などの代表シナリオをdocs/scenarios.mdに明文化する |
| Phase12 | 入力妥当性チェック | センサ・通信からの値を無条件に信用しない設計を導入する（物理的な限界値と故障値を混同しないよう注意） |
| Phase13 | DTO整理 | モジュール間・将来のCAN通信でやり取りする車両データの境界を整理する |
| Phase14 | Timer | 単純ループから周期処理（10ms/100ms/1s等）を意識した構造へ発展させる |
| Phase15 | Scheduler | Timerの延長として、ECU内部の周期処理をタスク単位に整理する |
| Phase16 | NVM強化 | persist.cを発展させ、保存/読み込み失敗・データ破損・デフォルト復旧等への対応を検討する |
| Phase17 | CAN通信 | 仮想CAN等を使い、ECU単体からECU間通信へ発展させる |
| Phase18 | CAN異常処理 | Timeout/Invalid Data等の通信故障をFault Detection→DTC→Loggerにつなげる |
| Phase19 | Watchdog | Schedulerが管理する周期処理の停止検出を導入する |
| Phase20 | ECU構造への再編成 | 機能が増えて責務の置き場所に迷いが出た段階でフォルダ構成を見直す（先行して変更しない） |
| Phase21 | 複数ECU | Engine ECUとDiagnostic ECU程度の小規模な協調動作を試す |
| Phase22 | Fault Injection | 故障を意図的に発生させ、シナリオを再現可能にする |
| Phase23 | Python結合テスト | Pythonからシナリオを投入し、期待結果と比較する結合テストを整備する |
| Phase24 | SILS | Python（シナリオ投入・故障注入・結果検証）とC ECUを組み合わせた自動検証を完成させる |

Phase9完了後、上記の中から次に着手するテーマを、判断順序（①〜④）に沿って改めて検討する。

### 付随テーマ（優先順位未定、随時差し込む）

上記の主な流れとは別に、実装を進める中で必要になったタイミングで差し込むテーマ。

- **Error Handlingの拡張**：Config読み込み失敗・CAN通信失敗など、失敗しうる処理が増えるたびに自然と積み増す
- **State Machineの汎用化**（ignition.cのOFF/ON限定から多値・汎用フレームワークへ）：CANの接続状態管理など、ignition.c以外に状態遷移が要る場面が出たときに一般化する動機が生まれる
- **診断セッション制御／複数DTC優先度付け**（diag.c/cmd.cの延長、day20.mdの「次回やること」の続き）：いつでも着手できる、優先度低め
- **テストカバレッジ計測（gcov/lcov）**：テストが一定量たまった節目でまとめて導入
- **ビット演算**（車載のレジスタ・プロトコル操作で頻出）：`SensorStatus`等の内部データをビット詰めする動機（メモリ削減）は外部境界（通信・永続化）が無いと固められない。実際のDTCステータスバイト（ISO 14229等）は本プロジェクトの案とは異なるビット配置であり、先に詰めても後で規格に合わせて作り直す可能性が高いため、CAN実装後に実際へ送信するデータのビット詰めとして再検討する
