# Day11

## 実施内容（進めた作業を自分の言葉で）

- loggerの設計案を比較し、可変長引数を使わない案（`snprintf`で文字列を組み立ててから`log_print`/`log_print_tagged`に渡す設計）を選んだ
- 理解しやすいようにsensor.c → status.c → stats.c → alert.c → diag.cの順でloggerに接続した
- printfとloggerの違い・メリットの所在（実行結果ではなく変更箇所の集約）を確認した

## 確認・検証

| 確認観点 | 結果 |
| --- | --- |
| ビルド（-Wall -Wextra -std=c11） | |
| テストビルド（make test） | |
| 実行確認（表示が従来通りか） | |
| `grep -nw printf` での確認 | |

## 次回の設計方針

-現状の学習における抜け漏れの補完を優先する（エラーハンドリング→ビット演算→文字列操作）

## 次回やること

-現状の学習における抜け漏れの補完
