# Day1

## 実施内容

- Cursor 導入
- GitHub 連携
- WSL2 Ubuntu 環境確認
- gcc 環境確認
- センサシミュレータ初回実装
- Makefile 作成
- ビルド確認
- 実行確認

---

## 変更・追加したファイル

| ファイル | 変更内容 |
| --- | --- |
| `include/sensor.h` | `VehicleSensorData` struct と関数宣言を追加 |
| `src/sensor.c` | `sensor_init` / `sensor_update` / `sensor_print` を実装 |
| `src/main.c` | 初期化・20回ループ・表示を実装 |
| `Makefile` | ビルド・実行・クリーンのターゲットを追加 |

---

## 確認・検証

ビルド: `-Wall -Wextra -std=c11` 警告・エラーなし

| 確認観点 | 結果 |
| --- | --- |
| ビルド | 成功 |
| 実行 | 20サンプル分のセンサ値が表示された |

---

## 気づき・メモ

- struct を使うと関連するデータを1つにまとめて扱える
- .h ファイルは他のファイルに関数を公開するための宣言
- Makefile があると毎回長いコマンドを打たずにビルドできる

---

## 次回の設計方針

- 統計機能は sensor.c とは別モジュール（stats.c）にする
- sensor.c の責務をセンサ値の更新・表示に限定するため

---

## 次回やること

- `&` とポインタの意味をもう少し確認する
- 統計モジュールの追加
