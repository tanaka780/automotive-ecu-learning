# 学習ジャーナル

理解した内容をテーマ別に整理するメモ。日報とは別に、後から見返しやすい形で残す。

---

## C言語基礎

### struct

関連するデータをまとめる型。speed / rpm / temperature を1つにまとめることで、関数に渡すときに1つ渡すだけで済む。

### typedef

`struct VehicleSensorData` と毎回書かずに `VehicleSensorData` だけで使えるようにする仕組み。型に短い別名をつける。

### .h ファイル

他のファイルに関数を公開するための宣言を書く場所。実装（中身）は .c に書く。

### Makefile

ビルドを自動化する仕組み。`make` だけで複数ファイルのコンパイルをまとめて実行できる。

### & とポインタ

- `&sensor_data` は変数に自動で割り当てられた住所（番地）を調べる演算子
- `VehicleSensorData *data` は住所を受け取るための変数（ポインタ）。値ではなく住所そのものが入っている
- 値をそのまま渡すとデータのコピーが作られる。コピーを書き換えても元データは変わらない。住所を渡すと元データを直接書き換えられる

### -> 演算子

住所（ポインタ）の先にある箱の特定のメンバーにアクセスする。読み取りにも書き込みにも使う。
例: `data->speed` → data に入っている住所の先にある speed にアクセスする

### const ポインタ

`const VehicleSensorData *data` はポインタ経由での書き込みを禁止する。
「読むだけ」の責務を型で明示するために使う。

### 整数除算と型変換

- `int ÷ int` はコンパイラが演算時点で int として計算する（小数点以下は切り捨て）
- `float result = int_a / int_b` は int 除算の後に float に変換するため、結果は小数にならない
- float の精度が必要な場合は `(float)int_a / int_b` のようにキャストが必要

### enum（列挙型）

複数の状態を名前で表す型。数値（0, 1, 2）をそのまま使うと意味が伝わらないため、
`LEVEL_NORMAL` / `LEVEL_WARNING` / `LEVEL_CRITICAL` のように名前をつける。
typedef と組み合わせて `SensorLevel` という型名をつけることができる。

### static 関数

`static` をつけた関数は、そのファイルの中だけで使える内部専用関数になる。
外部に公開しない補助関数（ヘルパー関数）には static をつけるのが C 言語の慣習。
status.c の `classify_level` と `level_to_str` はこのパターンで実装した。

### bool（<stdbool.h>）

C99以降で使える標準の真偽値型。`true`/`false`で意味がそのまま読み取れる。
0/1のような数値だけだと、どちらが「成立」でどちらが「不成立」か型からは分からないため、
enumで状態に名前をつけたのと同じ発想で、フリーズフレームの「記録済みか」を表す`captured`フィールドに使った。

### static変数（ファイルスコープ）

`static`は関数だけでなく変数にも付けられる。ファイルの先頭で`static int g_pass = 0;`のように書くと、
そのファイル内の関数からだけアクセスできる状態を持てる。test_diag.cではテストのPASS/FAIL件数を
集計するために使った。

### 1つのMakefileで複数の実行ファイルをビルドする

`TARGET`/`TEST_TARGET`のように、ビルドしたい実行ファイルごとに変数とルールを分けて書くことで、
1つのMakefileから本体（sensor_sim）とテスト（test_diag）を別々にビルドできる。

### 固定幅整数型

車載 C では `int` の代わりに `<stdint.h>` の `uint8_t` / `uint16_t` 等を使う慣習がある。
`int` はプラットフォームによってサイズが変わるが、`uint8_t` は常に 8 ビット符号なしと確定するため、
値の範囲と符号を型で明示することで意図しない型変換やオーバーフローを防ぐ。

#### 実装での型選定（Day5）

| フィールド | 値域 | 選定型 | 根拠 |
| --- | --- | --- | --- |
| `speed` | 0〜120 km/h | `uint8_t` | 最大 255 で十分 |
| `rpm` | 0〜6000 rpm | `uint16_t` | 6000 > 255 なので 8 ビットでは足りない |
| `temperature` | 25〜100 ℃ | `uint8_t` | 最大 255 で十分 |
| `speed_sum` | 最大 20×120=2,400 | `uint16_t` | 最大 65,535 で十分 |
| `rpm_sum` | 最大 20×6000=120,000 | `uint32_t` | 120,000 > 65,535 なので 16 ビットでは足りない |
| `temp_sum` | 最大 20×100=2,000 | `uint16_t` | 最大 65,535 で十分 |

**min 初期値の変更**:
元の `INT_MAX` は `int` 向けの最大値だった。`uint8_t` 型には `UINT8_MAX`（255）、
`uint16_t` 型には `UINT16_MAX`（65535）を使う。これらは `<stdint.h>` で定義されている。

**printf でのキャスト**:
`uint32_t` を `%d` に渡すと未定義動作の可能性があるため `(int)` キャストを明示する。
`uint8_t` / `uint16_t` は可変長引数渡し時に自動昇格されるが、意図を明示するため同様にキャストした。

### 可変長引数（variadic arguments）

呼び出すたびに引数の個数・型を変えられる関数の仕組み。`printf`が代表例。
`<stdarg.h>`の`va_list`/`va_start`/`va_arg`/`va_end`を使って実装する。
コンパイラは呼び出し側の型・個数をチェックできないため、フォーマット文字列と実引数の整合性はプログラマの責任になる。

loggerの設計時、可変長引数を使う案（`log_write("...", 値)`のようにprintfに近い書き方ができる）と、使わない案（呼び出し側で`snprintf`により文字列を組み立ててから渡す）を比較し、車載関連プログラムとしてより適切な方（可変長引数を使わない案）を選んだ。
可変長引数はコンパイラによる型チェックが効かず、環境（コンパイラやプラットフォーム）によって動作に差が出る可能性があるため、車載業界のコーディング規約MISRA Cでも使用が制限されている。

### snprintf

`printf`と同じフォーマット指定の書き方をするが、出力先が画面ではなく指定したバッファ（配列）になる関数。
第2引数にバッファサイズを渡すことで、書き込みすぎ（バッファオーバーフロー）を防げる。
logger導入時、各モジュールが`printf`を直接呼ぶ代わりに、`snprintf`で1行分の文字列を組み立ててから
`log_print`/`log_print_tagged`に渡す設計にした。

### strcmp による文字列比較

`<string.h>`の`strcmp`は2つの文字列を1文字ずつ比較し、完全に一致すれば0を返す。
診断コマンド入力（cmd.c）で、読み込んだ文字列が`"clear"`かどうかを判定するために使った。
`==`は文字列（char配列の先頭アドレス）そのものを比較してしまうため使えない。

### sscanf によるトークン分割

`sscanf(line, "%15s %15s %15s", a, b, c)`のように書くと、空白区切りの単語（トークン）を
最大3個までバッファに取り出せる。戻り値は実際に読み取れた個数。
`strtok`と違い元の文字列を書き換えないため、`const char *`の引数からでもトークンを取り出せる。
`cmd_dispatch`で`"clear speed"`のような2語のコマンドを解釈するために使った。

### sscanfのスキャンセット指定（%[^=]）

`%[...]`は指定した文字集合に一致する間だけ読み込む変換指定子。`^`を先頭につけると「指定した文字以外」の意味になる。
`sscanf(line, "%63[^=]=%d", key, &value)`は、`=`が出てくるまでの文字列を`key`に、続く整数を`value`に読み込む。
`config.c`で`"KEY=VALUE"`形式の設定ファイルを解釈するために使った。空白を含む文字列も読み込める点が、単語区切りの`%15s`（cmd.c）との違い。

### || の短絡評価と未初期化変数

`if (n < 1 || strcmp(command, "clear") != 0)`のように書くと、左側`n < 1`が真の時点で
右側`strcmp`は評価されない（短絡評価）。`sscanf`が1個もトークンを読めなかった場合
（`n == 0`）、`command`配列は書き込まれておらず中身が不定なため、先に`strcmp`を評価すると
未定義動作になる。`n < 1`を左に置くことで、未初期化のまま`command`を読む事態を避けられる。

---

## 設計パターン

### 責務分割

1つのモジュール（.c ファイル）が持つ仕事を1つに限定する。

- `sensor.c`：今この瞬間のセンサ値を管理する
- `stats.c`：これまでの値の最小・最大・合計を管理する
- `alert.c`：閾値チェックと警告出力のみを担当する
- `status.c`：センサ値の状態分類（NORMAL / WARNING / CRITICAL）と表示を担当する

混ぜると「このファイルは何をするファイルか」を一言で説明できなくなる。
また、一方を修正したときにもう一方に影響するリスクが生まれる。

### alert.c と status.c を分けた理由

一見、どちらも「閾値を超えたら何かする」に見えるが、責務が異なる。

- `alert.c`：閾値を超えた瞬間に警告を出力する（即時出力、2値判定）
- `status.c`：センサ値を3段階に分類し、毎周期の状態として表示する（常時表示、状態管理）

`SensorStatus` struct はセンサの生の値は持たず、分類結果（SensorLevel）だけを持つ設計にした。
こうすることで status.c の責務が「分類と表示」だけに絞られ、センサ値を直接触ることがない。

### 状態遷移の検出（エッジ検出）

「CRITICALである」ことと「CRITICALになった瞬間」は別の情報。後者を検出するには、前回の状態と今回の状態を比較する必要がある。

最初はこの比較用データ（previous/current）をstatus.cに持たせたが、status.cの責務は「センサ値を渡されたらその場で分類するだけ」だったため矛盾すると気づき、元に戻した。前回の状態は、それを使う側（diag.c）が自分の中で保持する設計にした。

### 並行変数（parallel variables）

`speed_crit_count` / `rpm_crit_count` / `temp_crit_count` のように、同じ意味の変数をセンサ別に名前だけで区別する書き方。

センサが増えるたびに変数と処理が増え、同じロジックを手書きで繰り返すことになる。enum + struct + 配列にまとめてループで処理する設計に直した。

### DTCの状態区分を2値でなく3値にした理由

`ACTIVE`/`HISTORY`の2値だけだと、「一度もCRITICALになっていない」センサも
どちらかの値を持つことになり、意味が曖昧になる。`DTC_NONE`を加えて3値にすることで、
「まだ発生していない」を型の値として正しく表現できるようにした。

### フリーズフレームのスコープ（センサ毎ではなく全体で1件）

フリーズフレームの目的は「異常発生時に他のセンサがどうだったか」を確認することなので、
発生したセンサだけでなく全センサ値を保存する必要がある。また「1件だけ保存する」という
規模に合わせ、センサ毎に持たせず、DtcRecord全体で1つだけ持たせる設計にした。

### 機能追加によって既存モジュールの責務が広がるとき

diag.cは元々SensorStatus（分類結果）だけを読む設計だったが、フリーズフレームの追加で
VehicleSensorData（生の値）も読む必要が生じた。新しい責務を既存モジュールにそのまま足すか、
別モジュールに分けるかは、そのモジュールの目的（診断情報の記録）と矛盾しないかで判断する。

### DTCカウントとフリーズフレームを分割しなかった判断

diag.cを「DTCカウント（SensorStatusだけを読む）」と「freeze_frame.c（VehicleSensorDataだけを読む）」
に分割することも検討したが、見送った。理由は次の2つ。

- どちらも同じエッジ検出（前回/今回のSensorStatus比較でCRITICALに入った瞬間）に依存しており、
  分割してもfreeze_frame.c側に同じ比較ロジックを重複させるか、diag.cから検出結果を渡すことになる。
  結合が消えるわけではなく、ファイルをまたぐ形に変わるだけ
- 実際のUDS/OBD診断でも、フリーズフレームはDTCレコードに付随するスナップショットとして
  一体で管理される。diag.cが両方持つ今の形は実車の診断データモデルに近い

分割を見直すのは、フリーズフレーム側だけが独自に複雑化する具体的な要求（例：DTCごとに複数件保持、
フリーズフレームだけ永続化）が出てきたとき。

### 出力フォーマットを無理に統一しない判断

sensor_print / stats_printはタグなし、status_print / alert_check / diag_printは`[STATUS]`等の
タグ付き、という違いがある。logger（出力層の共通化）を見据えて表示を揃えることも検討したが、
見送った。

- タグなしの2つは「今の値・集計値そのもの」を表示するテレメトリ、タグ付きの3つは
  「状態分類・警告・故障記録」という診断イベントの通知であり、役割が異なる。
  タグの有無はその違いを表しており、揃えない方が実は理にかなっている
- 見た目を変えるだけの変更は避ける方針（CLAUDE.md）にも反する

logger側を作るときは、表示を統一するのではなく、logger関数のインターフェースをタグあり/なし
両方に対応させる設計にする。

### テストで assert() ではなく自作の集計関数を使った理由

assert()は条件が偽になった瞬間にプログラムが終了してしまい、それ以降のテストが実行されない。
1回の実行で全部のテスト結果を確認したかったため、PASS/FAILを表示して件数を数える
自作の`check()`関数を使った。

### テスト専用ファイル名とsentinel値（test_persist.c）

persist.cの自動テストでは、本番の`dtc_data.txt`を壊さないよう、`test_dtc_data.txt`という
テスト専用のファイル名を使い、テスト終了時に削除する設計にした。

また、読み込み失敗時（ファイルが無い・内容が壊れている）に`dtc`が変更されていないことを
確認するため、`set_sentinel()`で見分けやすい値（count=42など）を事前に設定してから
`persist_load_dtc`を呼ぶ方式にした。「失敗時に何も書き換えていないか」は、初期値のままでは
区別がつかないため、わざと目立つ値を仕込んでおく必要があった。

### min/maxの初期値を型の最大値にする定石

`stats_init`では`speed_min`/`rpm_min`/`temp_min`を0ではなく型の最大値
（`UINT8_MAX`/`UINT16_MAX`）で初期化する。最初のサンプルが必ずmin側を更新できるようにするための
定石で、max側は逆に0で初期化することで同様に最初のサンプルで必ず更新される。
`test_stats.c`では、この初期値そのものを`stats_init`直後の状態としてテスト対象にした。

### 整数除算による平均の切り捨てをテストで固定する

`stats_print`の平均計算は`sum / count`という整数除算のため、小数点以下が切り捨てられる。
`test_stats.c`ではあえて割り切れない値（rpm合計7000を3で割るなど）を投入し、`2333`
（切り捨て）になることを明示的にテストして、この挙動が意図した仕様であることをテストとして固定した。

### 重複コードを共通化してよいかの判断基準

見た目が似ているコードでも、型やロジックが本質的に違う場合（stats.cのmin/max/sum）は
無理に共通化しない方がよい。逆に、型もロジックも完全に同じで値だけが違う場合
（test_diag.cの各サンプル入力）は、共通化しても複雑さが増えないため、ヘルパー関数にまとめた。

### #define マクロ

定数に名前をつける仕組み。コンパイル前にプリプロセッサが数値に置き換える。
`ALERT_SPEED_MAX` のように書くことで、数値の意味がコードから読み取れる。
閾値を変更したいときは .h の定義箇所だけを修正すればよい。

### logger設計（可変長引数を使わない案）

`printf`をモジュールごとに直接呼ぶ代わりに、各モジュールが`snprintf`で1行分の文字列を組み立て、
`log_print`（タグなし）/`log_print_tagged`（タグ付き）に渡す設計にした。
sensor.c → status.c → stats.c → alert.c → diag.c の順で移行し、
「タグなし単発」「タグ付き単発」「複数行」「条件付き複数回」「ループ＋分岐」と、
複雑さの低いものから1つずつ新しい要素を検証してから次に進んだ。

### 出力の集約と責務の分離

sensor.c/stats.c/alert.c/status.c/diag.cはそれぞれ「何を表示するか」だけを担当し、
「どう出力するか」はlogger.cに一本化した。
表示内容を1文字も変えない設計にしたため、実行結果（画面の見た目）からは
printfのときとloggerのときで違いが分からない。
メリットは実行結果ではなく構造に現れる。出力方法を変えたい場合、
logger.cの2関数だけを直せば全モジュールに反映される、という変更点の集約がメリット。

### ロジックとI/Oの分離（persist.c）

DTCをファイルに保存する機能で、「DtcRecordを文字列に変換する処理」と
「実際にファイルを読み書きする処理」を別の関数に分けた。
logger.cで実践していた「文字列を組み立てるのは各モジュール、実際の出力はlogger.cのみ」
という分離を、ファイルという新しい出力先にもそのまま応用した形。
変換処理はファイルを一切扱わないため、実ファイルを使わずに変換の正しさだけを確認できる。

### 保存モジュールを汎用化するかDTC専用にするか

「DtcRecordの変換ロジックはdiag.c、ファイルI/Oは汎用のpersist.c」という2層構成の案と、
「persist.cがDTC専用の保存・読込関数を持つ」という1モジュールの案を比較した。
前者は将来別のデータ（統計など）も保存したくなったときに再利用しやすいが、
現時点でDTC以外に永続化したい対象が無いため、後者を選んだ。
今無い要件のために汎用化しておくのではなく、2つ目の永続化対象が実際に出てきたときに
初めて汎用化を検討する、という判断。

### 保存形式（テキスト vs バイナリ）のトレードオフ

構造体をそのままファイルへダンプするバイナリ形式は実装がシンプルだが、
中身を目視確認できず、構造体にフィールドを追加すると過去のファイルと互換性が崩れる。
snprintf/sscanfで組み立てるテキスト形式は変換の手間がある分、
中身を`cat`で確認でき、デバッグしやすい。
初めてファイルI/Oを扱う今回はテキスト形式を選んだ。

### 標準出力キャプチャ（freopen + dup/dup2）

`alert_check`や`ignition_check`は判定結果を構造体に書き込まず標準出力に表示するだけのため、
`diag.c`/`persist.c`/`stats.c`のテストで使ってきた「構造体の中身をcheck()で確認する」手法が使えない。
そこで、表示されるはずの文字列そのものを一時ファイルに書き出させて読み戻す「標準出力キャプチャ」を導入した。

`freopen(filename, "w", stdout)`で標準出力の行き先を一時ファイルに差し替えられるが、
それだけだと元の端末に戻す手段が残らない。差し替え前に`dup(STDOUT_FILENO)`で元のfdを複製して
手元に保存しておき、差し替え後の呼び出しが終わったら`dup2`でそのfdをSTDOUT_FILENOに書き戻すことで、
以降の`printf`は再び画面に出力されるようになる。

「構造体で結果を確認する」パターンと「出力そのものを確認する」パターンでは、
後者の方がテストの手間が1段階多い（差し替え・復元・ファイル読み戻しが必要）ことを実感した。

### 既存の分類結果を配列化して回避コードを解消した例（SensorStatus）

`SensorStatus`はもともと`speed_level`/`rpm_level`/`temp_level`という個別フィールドを持っていたため、
diag.cがセンサ別にループ処理する際、個別フィールドを一時配列に詰め替えるコードが必要になっていた。
`SensorLevel levels[SENSOR_COUNT]`という配列に変更し、添字を`SensorId`（sensor.hで定義）と共有する
ことで、diag.c側の詰め替えコードが不要になった。同じ型の3つの値を個別変数で持つと、
それを使う側に本来不要な変換コードが生まれる、という「並行変数」の実例。

### 既存パターンをそのまま別モジュールに流用する（ignition.c）

イグニッション状態管理（ignition.c）を新規実装する際、状態更新のロジックはsensor.cの
`sensor_update`（ランダム値で更新する）パターンを、遷移検出のロジックはdiag.cの`diag_check`
（前回/今回比較でエッジを検出する）パターンをそのまま流用した。新しいテーマ（電源状態）でも、
値の更新と状態比較という処理の形はセンサ値やDTCと同じだったため、新しいロジックパターンを
増やさずに実装できた。

### 重複コードを共通化しなかった判断（test_alert.c / test_ignition.c）

標準出力キャプチャの仕組みが2ファイルでほぼ同じ形になったため、関数マクロや関数ポインタでの
共通化を検討した。当初はMISRA C（Directive 4.9、関数マクロより実関数を推奨）を見送りの根拠に
しようとしたが、これは実車に搭載される量産コード（src/）向けの指針であり、test/配下の
テストハーネスには本来当てはまらないと気づき、根拠から外した。
実際の見送り理由は、`alert_check`と`ignition_check`で引数の型が異なり、関数ポインタでの
共通化は型不一致のリスクがあること、関数マクロなら型を気にせず書けるが型チェックが効かず
引数が複数回評価されるリスクがあることを踏まえ、重複が2箇所・15行程度に留まる段階では
共通化の複雑さの方が重複のコストを上回ると判断したこと。「重複コードを共通化してよいかの
判断基準」の実例の1つ。

### 新規モジュールの責務をA/B/C軸で分けて設計する（cmd.c）

診断コマンド入力を実装する際、(A)コマンドをいつ受け付けるか、(B)どのモジュールに実装するか、
(C)persist.cとの整合をどう取るか、という3つの軸に分けて複数案を比較してから設計した。
(B)では、新規モジュール`cmd.c`に「標準入力を読む処理」と「文字列を解釈する処理」を持たせ、
実際に`DtcRecord`を書き換える処理は既存の責務分割どおり`diag.c`に残した。
1つの機能追加でも複数の論点が混ざっている場合は、軸で分けて個別に比較すると判断しやすくなる
ことを実感した。

### 同じ結果に至る初期化処理を重複させない（diag_clear）

`diag_clear`（クリアコマンドによるリセット）と`diag_init`（起動時の初期化）は、最終的に
到達する状態（DTC記録・フリーズフレームが未発生の初期状態）が完全に同じだったため、
`diag_clear`の中で`diag_init`を呼び出す形にし、同じリセット処理を2箇所に書かないようにした。

### 1件しか持たない共有データへの部分操作（フリーズフレームの部分クリア）

`FreezeFrame`は全センサ共通で1件しか持たない構造のため、「センサ1件分だけクリアして」と
言われたときにフリーズフレームをどうするかは自明ではなかった。「常に消す」でも「常に残す」
でもなく、フリーズフレームの原因センサ（`trigger_sensor`）とクリア対象のセンサが一致する
場合だけ合わせてクリアする、という判定にした。無関係なセンサをクリアしたときに別センサの
記録まで消えてしまう不自然な挙動を避けるための設計判断。

### グローバル状態を避けて明示的に引数で渡す（ConfigDataの注入）

`alert_check`/`status_check`は`alert.h`/`status.h`のマクロを直接参照するのをやめ、
`const ConfigData *`を引数で受け取る形にした。グローバルなconfig変数を用意して各関数が
直接参照する案も検討したが、グローバル状態はテスト側から任意の値に差し替えにくく、
テストの独立性を壊しやすい。呼び出し元（`main.c`やテストコード）が明示的に値を渡す設計に
することで、テストごとに異なる`ConfigData`を注入できるようにした。

### 「読み込める」ことと「判定に反映される」ことは別の検証観点

config.cの設定ファイル読み込みが動くことと、読み込んだ値が`alert_check`/`status_check`の
判定に実際に使われることは別の観点だと気づいた。デフォルト値と同じ設定でしかテストして
いないと、「値は読めているが判定には使われていない」バグを見逃せてしまう。非デフォルトの
`ConfigData`を渡すテストケースを追加して初めて、この観点を検証できた。

### 自動テストでの裏付けと、実際に動かして確認することは別

`config_load`のファイルパース（`test_config.c`）と、読み込んだ値が判定に反映されること（`test_alert.c`/`test_diag.c`）はそれぞれ自動テストで確認済みだったが、この2つを実際の`config.txt`1枚でつなげて`make run`したことは一度も無かった。「テストが通っているから動くはず」と「実際に動かして確認した」は別の話だと気づいた。実際に`config.txt`を作って動かすと、閾値通りに`ALERT`/`STATUS`の出力が変わることを目で確認でき、そこで初めて「確認済み」と言える状態になった。

### ログの「タグ」と「レベル」を分ける設計

出力先を示す`タグ`（`ALERT`/`DTC`等）と、メッセージの重要度を示す`レベル`（`LOG_INFO`/`LOG_WARNING`/`LOG_ERROR`）は別の分類軸である。1つのタグの中に複数の重要度が混在する場合（`PERSIST`タグは保存成功がINFO相当、保存失敗がERROR相当）があり、タグだけでは重要度を一意に決められない。そのため`log_print_tagged`とは別に、レベル・タグ・メッセージの3つを受け取る`log_print_leveled`を新設し、呼び出し側がログ1つずつにレベルを指定する設計にした。

### 設定を読み込む処理自身のログは、その設定で制御できない

`config.c`の`log_level`は`config_load`を実行して初めて確定する値だが、`config_load`自身も「設定ファイルが見つかった/見つからなかった」というログを出す。この2つのログを`log_print_leveled`にすると、自分自身がこれから決めようとしている閾値で自分の出力可否を判定することになり、実際には`logger_set_level`が呼ばれる前（＝前回の閾値かデフォルト値）で評価されてしまう。この2箇所だけは意図的に`log_print_tagged`（無条件で常に表示）のまま残した。

### ログの重要度は「達成できたか」ではなく「問題が起きたか」で判断する

`PERSIST`のログレベルを検討する際、当初「保存/読み込みが目的を達成できなかった場合はERROR」という基準をそのまま当てはめ、`persist_load_dtc`が`false`を返す2パターン（ファイルが無い＝初回起動、データが壊れている）を両方ERRORにした。しかし「初回起動でファイルが無い」のは誰にでも起きる正常な状態であり、実際の異常（保存失敗・データ破損）と同じ重篤度にするのは実態に合わない。戻り値（達成できたか）だけでなく、その状況が実際に問題かどうかで重要度を判断する必要があると気づいた。

### トークン化(sscanf)と完全一致(strcmp/strncmp)の使い分け

`cmd_dispatch`は当初`sscanf("%15s %15s %15s", ...)`でトークンに分割していたが、`%s`は前後の空白を自動的に読み飛ばすため、「前後の空白もトリムしない完全一致」という仕様と実装がずれていた（`" clear"`のような入力も`"clear"`として通ってしまっていた）。仕様が多少の表記ゆれを許容するなら`sscanf`のトークン化が向くが、厳密な一致だけを受け付けるなら`strcmp`/`strncmp`で直接比較する方が仕様通りになる、という使い分けに気づいた。

### 状態の切り替えを暗黙の条件ではなく明示的なフラグで持たせる

`fixture.c`の固定値/ランダムの切り替えを、当初「ファイルの有無」で判定する案を検討したが、削除し忘れによる意図しない動作のリスクがあると気づいた。`MODE=FIXED`/`MODE=RANDOM`という明示的なキーに変更し、常に同じファイルを同じ手順で読み、中身のフラグだけで分岐する設計にした。ファイルが存在するかどうかのような暗黙的な状態より、今どちらのモードかを明示的に書く方が、テストの前提条件として安定する。

### 「自動検証」と「可視化」は目的が違う

Pythonでシナリオの結果をグラフ化する案を検討したが、グラフは最終的に人間が見て判断する必要があり、「人間の確認作業を無くす」という自動検証の目的とは噛み合わないことに気づいた。同じ「Pythonで結果を確認する」でも、機械的にPASS/FAILを判定することと、人間が見て判断することは別の目的であり、混同しないよう整理した。

### 値域チェックを一元化するかの判断基準（persist.cの前例の応用）

persist.cを汎用化しなかった判断基準（「2つ目の対象が実際に出てきたときに初めて汎用化する」）を、config.c・fixture.cという2つの呼び出し元が既に存在するvalidate.cの設計（新規モジュールへの一元化）に適用した。

### ツール導入の判断軸も機能追加と同じにする

テストフレームワーク（Unity）や静的解析（cppcheck/MISRA）のようなツール導入も、機能追加のときと同じ判断順序（①ストーリー上の必要性→②既存モジュールへの影響→③テスト方法→④Phase化の要否）で評価できると気づいた。特に②の影響範囲（例：MISRA対応で単一出口ルールがほぼ全モジュールに影響する）を先に見積もることで、1つのPhaseにまとめるべきか複数に分けるべきかを判断できる。

### 横断的な変更は「1日で終わるか」ではなく「段階的に検証できるか」で範囲を決める

MISRA対応のように影響範囲が広い変更を1つのPhaseにまとめてよいか迷ったが、Phase3（logger導入）の前例（モジュールごとに移行し都度ビルド・テスト・実行確認する）を思い出し、「1回の作業を小さくする」は「1日で終わらせる」という意味ではなく「1つ1つのタスクを検証可能な単位に刻む」という意味だと気づいた。

---

## 車載ソフトウェアの考え方

### センサ値の物理的な範囲

センサ値の範囲は「実際の車がどこまで動くか」から決める。

- speed: 0〜120 km/h は停車〜一般道の上限
- rpm: 800〜6000 はアイドリング（エンジンがかかっている最低回転）〜レッドライン（上限）
- temperature: 25〜100℃ は常温〜冷却水が沸騰する手前

範囲が決まると「この型で足りるか」を計算できる。

### 実際のECUとシミュレーションの違い

実際の ECU はセンサや CAN という車内の通信線から値を読み取る。
このプロジェクトの `sensor_update()` はその部分を `rand()` のランダム値で代わりにやっている。

### 複数アラートの同時発火

`alert_check` の `if` を `else if` にすると、speed が引っかかった時点で rpm と temperature のチェックが飛ばされる。
`if` を3つ並べることで、速度・回転数・水温が同時に閾値を超えてもそれぞれ警告を出せる。
実際の車でも複数の異常が同時に起きることはある。

### 段階的なアラート設計

alert.c の単一閾値は「超えた / 超えていない」の2択でシンプルだが、緊急度の区別ができない。
status.c の2段階閾値にすると NORMAL / WARNING / CRITICAL を区別でき、注意段階で早めに気づける。
実際の ECU でも DTC（故障診断コード）は重篤度が複数段階に分かれている。

### DTC（故障診断コード）の基本的な考え方

DTCは「閾値を超えた」という事実そのものではなく、「異常が新たに発生した」というイベントを記録するもの。CRITICALが続く間ずっと記録すると、本来1件の故障が何十件にも増えてしまう。

前回の状態と今回の状態を比較し、NORMAL/WARNINGからCRITICALに変わった瞬間だけを記録することで、実際のECUの故障診断に近い動作になる。

### フリーズフレームの車載的な意味

実際の車載診断では、異常が発生した瞬間の車両状態（速度・回転数・水温など）を
スナップショットとして残す。発生要因になったセンサの値だけでなく、他のセンサの値も
分かることで、原因の切り分けに役立つ。

### DTCのactive/history概念

DTCには「現在も故障が継続中（active）」か「過去に発生したが解消済み（history）」かの
区別がある。故障が今も起きているのか、一時的だったのかを見分けるための情報。

### 固定幅整数型の車載での意義

`int` は環境によってサイズが変わるため、プラットフォームが違うことが多い車において、`int` だと想定外の動きをするリスクがある。
`uint8_t` や `uint16_t` を使えば「何ビットか」「符号ありかなしか」が型の名前から分かる。

### 実機のログ設計（DLTなど）を採用しなかった理由

実際のECUでは、Flash容量・CPU・通信帯域の制約から、人間が読む文字列ではなくIDと数値データだけを送り、PC側の診断ツールで文字列に変換する設計（AUTOSARのDLT: Diagnostic Log and Traceなど）が使われることが多い。
また実機では`<stdio.h>`系の関数（`printf`/`snprintf`など）自体を使わない設計も多く、これは車載業界のコーディング規約MISRA Cで標準入出力関数の使用が制限されていることとも関係する。
このプロジェクトはPC上でstdoutに出力するだけで、Flash容量・CPU・通信帯域のいずれの制約も存在しないため、現状ではID+バイナリ方式や`printf`を避ける設計を導入する必要はないと判断した。
将来「実機の制約をシミュレートする」というテーマ自体を学びたくなったときは、独立した新しいPhaseとして扱う候補になる。

### NVM（不揮発性メモリ）とDTCの永続化

実車のECUはDTCを不揮発性メモリ（NVM/EEPROM）に保存し、電源が切れても診断情報を保持する。
このプロジェクトではファイルへの保存をNVMの代わりとして扱った。
プログラムを再起動してもDTCの発生回数やフリーズフレームが引き継がれることを実行確認し、
「電源断をまたいで保持されるべき情報」という考え方を実際の動作で確かめられた。

### エラーハンドリングが必要になる境界

これまでのモジュール（sensor.c、diag.cなど）はメモリ上の操作だけで完結しており、
実際に失敗しうる処理が無かった。ファイルI/Oという「プログラムの外側（ファイルシステム）」と
やり取りする処理が入って初めて、「ファイルが無い」「内容が壊れている」という
実際に起こりうる失敗を扱う必要が出てきた。
エラーハンドリングは「外部との境界」で必要になる、という考え方を実感した。

### 診断出力の検証可能性（テスト容易性）

実際のECUでも、DTCや診断イベントの出力は診断ツール側で検証対象になる。
`diag.c`/`persist.c`/`stats.c`のように結果が構造体に残るデータと、`alert.c`/`ignition.c`の
`ignition_check`のように一過性の出力でしか結果が分からないデータでは、自動テストの書きやすさが
大きく変わることを`test_alert.c`の実装で実感した。設計段階で「結果をどこに残すか」を決めることが、
後からテストしやすいかどうかにそのまま影響する。

### ビット演算が車載で使われる理由（メモリ節約ではなく通信仕様への準拠）

ビット演算というテーマ自体は保留にしたが、検討の過程で車載での位置づけを整理した。
`SensorStatus`のような内部データをビット詰めする動機は、実際には「メモリ節約」ではなく、
外部境界（他ECUや診断ツールとの通信、ファイルへの永続化）が無いと生まれない。
実際のDTCステータスバイト（ISO 14229等）は本プロジェクトの構造とは異なるビット配置で
あり、先にビット詰めしても後で規格に合わせて作り直す可能性が高いため、外部境界ができる
まで保留にした。

### 電源サイクルをまたぐDTC・統計の継続

イグニッションがOFF→ON→OFFと繰り返されても、DTCの発生回数・active/history状態、
統計データはリセットせず通算で継続する設計にした。DTCは実車でも電源断をまたいで
保持されるのが本質的な仕様であり、統計についても今回は同じ方針を採用した。

### ECUの通電状態の再現（イグニッションOFF中の処理スキップ）

イグニッションがOFFの間は、`sensor_update`以降の処理（センサ更新・状態判定・DTC記録・
警告・統計更新）を全てスキップするようにした。実車でもイグニッションOFF中はECUの
多くの機能が動作を止めるため、それを「OFF中は主要な処理を呼び出さない」という形で
再現した。

### UDS（統一診断サービス）のDTCクリアという概念の簡易再現

診断コマンド入力（`"clear"`）は、実際の車載診断規格UDS（ISO 14229）が持つ
「診断ツールからのDTCクリア要求」を簡易的に再現したもの。ただし実際のUDSと比べると、
サービスIDによるバイナリプロトコルではなくテキストコマンドである点、拒否理由
（ネガティブレスポンスコード相当）を区別しない点、セッション/認可の概念が無い点など、
多くの仕組みを省略している。

### UDSのgroupOfDTC・ネガティブレスポンスコードの簡易再現

`"clear <センサ名>"`によるセンサ単位のクリアは、UDSのgroupOfDTC（クリア対象の範囲を
指定する仕組み）の簡易再現。また、不正なセンサ名を`clear`に続けて指定したときに
専用のメッセージを返す仕組みは、UDSのネガティブレスポンスコード（拒否理由を区別して
返す仕組み）の簡易再現。どちらも実際の規格ほど細かい区別はできていない
（クリア範囲はセンサ単位のみ、拒否理由は「不正なセンサ名」の1パターンのみ）。

---

### キャリブレーションデータ（閾値）の外部化

実際のECUは、警告閾値のような値をコードに固定せず、書き換え可能なパラメータ（キャリブレーションデータ）として持つことが多い。開発中は値を調整し、量産後は書き込み保護する運用がされる。
`alert.h`/`status.h`にマクロで固定していた閾値を`config.c`経由でファイルから読み込む形にしたのは、この「制御ロジックとパラメータを分離する」考え方の簡易的な再現。

### リンプホームモード（読み込み失敗時のデフォルト動作）

設定ファイルが無い・壊れている場合にプログラムを止めるのではなく、コードに残したデフォルト値（`alert.h`/`status.h`の現行マクロ値）で動作を継続する設計にした。
実車でもキャリブレーションデータが読めない・異常なときにECUの機能を全停止するのではなく、安全側のデフォルト値で動作を続ける「リンプホームモード（縮退運転）」という考え方があり、その簡易的な対応関係になっている。
`alert.h`の`ALERT_SPEED_MAX 100`のようなマクロ自体は消えていないが、Config導入前後で役割が変わったことに気づいた。導入前は「実際に使われる運用値」だったが、導入後は「config.txtが使えないときのフェイルセーフの初期値」という別の役割に変わっている。同じ数字でも、コード中に置かれている意味が違う。

### ログの重要度レベル（INFO/WARNING/ERROR）

実際の車載診断ログ（AUTOSAR DLT等）では、出力先やコンポーネントを表すIDとは別に、メッセージの重要度（INFO/WARN/ERROR相当）を持つのが一般的。`LogLevel`（`LOG_INFO`/`LOG_WARNING`/`LOG_ERROR`）を導入し、`config.txt`の`LOG_LEVEL`で実行時の表示閾値を切り替えられるようにしたのは、この考え方の簡易的な再現。量産ECUでも「デバッグ時は全ログ、量産時はWARNING以上のみ」のような運用に近い（本プロジェクトはコンパイル時ではなく実行時の設定ファイルで切り替える点が異なる）。

### 固定値注入によるシナリオ再現（テストベクタ、HILS/SILSの入口）

実際の車載開発では、センサ値をランダムに動かすだけでなく、既知の入力（テストベクタ）を与えて既知の結果と比較する検証（HILS/SILS）が行われる。`sensor.c`の`rand()`は毎回異なる値になるため、「同じ入力で同じ結果になるか」を機械的に確認できない。正常走行・故障発生・電源再投入といった代表シナリオを、`config.txt`と同じKEY=VALUE形式の固定値で再現する仕組み（`fixture.c`、Phase11）を構築したのは、この考え方の入口にあたる。

実装してみると、値をサンプルごとに変化させる仕組み（シーケンス）を作らなくても、単一の固定値だけで故障発生シナリオを再現できることが分かった。`DtcRecord`の`previous`は起動時にNORMAL相当で初期化されるため、センサ値を1つのCRITICAL値に固定するだけで、初回サンプルが「NORMAL→CRITICAL」の遷移として検出され、DTC記録が発生する。既存のエッジ検出の仕組み（diag.cで学んだもの）が、新しい仕組み（固定値注入）にもそのまま活きた実例。

### ECU内部のデータと外部から来るデータの違い（config.txtとfixture.txtの分離）

実車のECUでは、閾値のようなキャリブレーションデータはECU自身のメモリに持つが、センサ値は配線やCAN経由で外部から入ってくる。この2つは物理的に別の場所にあるデータだと気づき、`config.txt`（閾値）と`fixture.txt`（センサ固定値）を1つのファイルに混ぜず、あえて別ファイルに分けた。同じ「KEY=VALUE形式の外部ファイル」でも、中身が判定パラメータかテスト入力データかで扱いを変えるべきだと分かった。

### レンジチェックとプラウジビリティチェックの違い、入力異常と故障を混同しない設計

値域チェック（Phase12、validate.c）を検討する中で、「型として物理的にありえる範囲か（レンジチェック）」と「意味的に妥当か（プラウジビリティチェック）」は別の検証観点だと気づいた。また、値域チェックは「入力として異常な値（壊れた・ありえない値、例: RPM=70000）」をはじくためのものであり、「入力としては正常だが車両として異常な値（例: Temp=95、既存のCRITICAL閾値90を超える本物の故障値）」まで拒否してはいけない。前者は入力の妥当性、後者はstatus.c/alert.cが担う故障検知という別の責務であり、両者を混同しない設計が必要だと理解した。

### 「エラーが出ない」ことと「車載的に妥当」なことは別の軸

`gcc -Wall -Wextra`やcppcheckの通常ルールは正しさ（バグの有無）を見るが、車載業界特有の書き方（MISRA C準拠）は別の軸であり、これらのチェックが通っても保証されない。cppcheckの`--addon=misra`で実際にチェックすると、単一出口(15.5)やif文の書き方(15.6/15.7)等、通常のチェックでは見えない指摘が多数出た。正しさ・効率・読みやすさ・車載での慣用性は、それぞれ別の軸で確認する必要があると実感した。

## まだ理解が浅いこと

- 現時点で未解決の項目はなし（diag.cの責務範囲、出力フォーマット統一の要否は結論を出し、上記「設計パターン」に反映済み）
